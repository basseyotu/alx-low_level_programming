#include <stdio.h>

/**
 * main - print alpha in lower and upper case
 * Return: Always 0
 */
int main(void)
{
	char ol;

	for (ol = 'a'; ol 'z'; ol++)
		putchar(ol);

	for (ol = 'A'; ol <= 'Z'; ol++)
		putchar(ch);

	putchar('\n');

	return (0);
}
