#include <stdio.h>

/**
 * main - print in single digits
 * Return: Always 0
 */
int main(void)
{
	int o;

	for (o = 48; n < 58; o++)
	{
		putchar(o);
		if (o != '57')
		{
			putchar(',');
			putchar(' ');
		}
	putchar('\n');
	return (0);
	}
}

